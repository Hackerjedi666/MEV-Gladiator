# Reference searcher bots

Run individual bots with Bun from the repo root:

```bash
pnpm --filter bots random
pnpm --filter bots arb
pnpm --filter bots sandwich
```

Copy `.env.example` to `.env` and set bot keys and coordinator URL first.
